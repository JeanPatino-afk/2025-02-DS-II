spring.application.name=plataforma_online

# --- Conexión con la base de datos ---
spring.datasource.url=jdbc:mysql://localhost:3306/tienda_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1043
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=create-drop

# --- JWT settings ---
# jwt.secret es la clave secreta para firmar y verificar los tokens JWT.
jwt.secret=6e8f2b1c-9a4d-4e7b-8c2f-3d1a5b7e2c4f!SuperClaveSegura2025
# jwt.expiration-ms define cuánto tiempo dura el token JWT antes de expirar (en milisegundos)
# y el usuario tenga que volver a iniciar sesión, en este caso, es una hora.
jwt.expiration-ms=3600000


# Puerto de la aplicación
server.port=9000

